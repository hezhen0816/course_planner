# 交接

先讀上層與本專案 AGENTS、CLAUDE，再讀本檔與既有待辦。
只在使用者說「寫交接」時寫；固定一份，覆寫更新不留舊版，寫完回報本檔路徑。讀完不立即清空，工作完成後才移除暫時事項。

## 2026-09-08 — Claude

- 實際專案路徑：`/Users/hezhen/Documents/Project/GitHub/course_planner`（GitHub `hezhen0816/course-compass`）。NTUST_Course_Monitor 已併入本 repo；其本機資料夾已丟垃圾桶、GitHub repo 封存、Vercel 與 Railway 專案刪除、Windows 排程工作解除登記。任何監控相關工作都在本 repo 做。
- 分支／HEAD：`main` @ `e504c84`，工作樹乾淨，與 origin 同步。
- 本輪完成與未提交：全部已提交並 push。本輪重點：Phase 0–4 合併（共用 Supabase、worker 搬入 `backend/monitor/`、前端「選課監控」頁、Web 回 Vercel `https://ntust-course-compass.vercel.app`、名稱統一為 course-compass）。決策與失敗經驗都在 `HISTORY.md` 2026-09-07／08 條目與 `docs/architecture/monitor-merge-plan.md`。
- 已驗證（2026-09-08 凌晨）：`npm run backend:test` 81 passed；`web:lint`、`web:build`、`tsc` 通過；Windows 上 `Course_Compass_Monitor` 與 `Course_Compass_Backend` 運行中、`logs\ntust_monitor.log` 零錯誤；心跳每位使用者每分鐘 1 筆（曾因 Railway 殘留 worker 變 2 筆，已排除）；worker 登入後 `app_private.school_sessions` 有寫入；Vercel 正式站以略過登入模式能開「選課監控」頁；後端 CORS 對 Vercel 網域回 `access-control-allow-origin`。
- 未驗證：登入後的「選課監控」頁實際資料流（需使用者帳號，代理不代輸入密碼）；iOS 對新 Supabase 專案的同步；Compass 官方初選是否真的復用 worker 寫入的 session（尚無人操作）。
- 暫時狀態：
  - 兩位使用者（heij82351、bocho960321）的校務帳號在 SSO 得到「Error. An error occurred while processing your request」錯誤頁，同一段程式對第三位帳號正常；worker 對他們每 15 分鐘試 3 次（連續失敗保護）。使用者說先暫時不管。
  - 三位由 admin 建立的帳號（jum60412、wanyong0925、a0909041576）拿到臨時密碼，尚未確認已改密碼。
  - Supabase 免費方案顯示「Grace period is over」，目前用量遠低於上限（egress 1.91/5 GB、DB 33/500 MB，2026-09-08 查），但已是硬上限，需定期看。
  - Mac 的 Tailscale 會在網路切換後停掉，連不到 `winhome` 時先 `tailscale up`。
- 下一步：使用者把新網址交給同學後，看 `TODO.md` P0（SSO 帳號鎖定保護已做一半：cooldown 有了，儀表板警示還沒；加選嘗試次數改存資料庫）。登入流程合一（`EnrollmentClient.login` 改用 `ntust_common.login_to_target`）在正式環境失敗過，重試前先離線比對兩套流程的請求差異，不要直接再上正式站。
- commit／push／部署／對外發布：本 repo 已 commit 並 push（e504c84）；Windows 後端與 worker 已部署到 e504c84 之前的程式版本（最後一次程式變動 6153f56 已部署，其後只有文件）；Vercel 由 push 自動部署，最新為 c812151 之後的文件 commit；Supabase 專案已改名 `course-compass`（ref 不變），Auth site_url 與白名單指向 Vercel 網域。無待辦的對外發布。

永久規則放 AGENTS，決策理由放 HISTORY，未完成工作放既有待辦。
數量、服務與額度標查證時間；不複製密鑰、不把歷史授權寫成任意後續授權。
